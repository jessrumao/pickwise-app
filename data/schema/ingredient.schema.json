{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "ingredient.schema.json",
  "title": "Ingredient (L1)",
  "description": "A delivery vehicle for one or more compounds. Holds identity and suitability only — NOT evidence (see claims/), NOT rules (see policy/), NOT dosing (see policy/dosing/). Fish oil and algal oil are two ingredients delivering the same compound; that is what makes substitution a query instead of a hand-written branch.",
  "type": "object",
  "required": [
    "id",
    "name",
    "category",
    "delivers",
    "suitableFor",
    "review"
  ],
  "properties": {
    "id": {
      "type": "string",
      "pattern": "^[a-z0-9-]+$"
    },
    "name": {
      "type": "string"
    },
    "category": {
      "type": "string",
      "enum": [
        "protein",
        "performance",
        "micronutrient",
        "gut_health",
        "food_based",
        "recovery_joint"
      ],
      "description": "UI grouping only. Never read by the rules engine."
    },
    "forms": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "delivers": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": [
          "compoundId"
        ],
        "properties": {
          "compoundId": {
            "type": "string"
          },
          "typicalConcentration": {
            "type": "number",
            "description": "Fraction of the ingredient by mass that is this compound, e.g. 0.78 for whey concentrate protein. Indicative only — the authoritative per-serving number lives on the PRODUCT, because that is what the user actually swallows."
          }
        }
      }
    },
    "suitableFor": {
      "type": "array",
      "items": {
        "type": "string",
        "enum": [
          "omnivore",
          "vegetarian",
          "eggetarian",
          "vegan",
          "pescatarian",
          "other"
        ]
      },
      "description": "Dietary patterns this ingredient is acceptable for. The substitution query is: which ingredients deliver compound X and list the user's dietaryPattern here?"
    },
    "allergens": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "review": {
      "$ref": "review.schema.json"
    },
    "notes": {
      "type": "string"
    }
  }
}
